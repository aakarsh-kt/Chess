import React from 'react';
import {useLocation} from 'react-router-dom';

const navigate=useLocation();

export const Button=()=>{
    return(
        <div>
            <h3>Button</h3>
        </div>

    )
}